/*
*	C++实现大数据四则运算。
*	class BigNum有两个成员变量和一个成员函数
*	当输入的数可以用系统long long范围表示时，保存在_value中，当超出范围时，用string形式表示在_strData中，
	并在最左边一位用'+''-'符号表示数字的正负
*	其中出现非数字字符时，只保存到出现之前的数字
*	当前面有0时，不采纳0
*	当输入的字符串有空格时，不采纳空格
*	重载了<<使得可以直接输出对象中的_strData
*	没有重载+=运算符，所以只能表示为 a = b + c;
*	只能做整形的运算，除法运算只保留整数部分
*	还有很多函数未写，比如重载 >>实现直接键盘输入值，+= 等以待后续开发

*	ZBT ljx 2019.12.17
   */
